We talk about syntax quickly first?
Make the psuedo code before they start the task


1. Temperature Converter
   Build a console app that converts temperatures between Celsius and
   Fahrenheit. The program should:

Ask the user which conversion they want (C to F, or F to C)
Take a temperature input
Display the converted result

Formulas:

C to F: (C × 9/5) + 32
F to C: (F - 32) × 5/9

## Kata 2 count string
string text = "programming";
char targetChar = 'g';
int count = 0;

// Your loop here

Console.WriteLine($"The character '{targetChar}' appears {count} times");
```

---

## Kata 2: Find Largest Number

**Task:** Find the largest number in an array of numbers.

**Example:**
```
Input: [12, 4, 45, 7, 5, 34]
Output: The largest number is 45